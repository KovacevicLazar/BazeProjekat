<Window x:Class="Intervencije_VatrogasnihJedinicaUI.Views.VatrogasneJediniceView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:cal="http://www.caliburnproject.org" xmlns:models="clr-namespace:Intervencije_VatrogasnihJedinicaUI.Models" mc:Ignorable="d" FontWeight="Bold" WindowStartupLocation="CenterScreen" Title="Vatrogasne Jedinice" Height="450" Width="800">
    <Window.Resources>
        <models:NullToMessageConverter x:Key="NullToMessageConverter" />
    </Window.Resources>
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding SveJedinice, Mode=OneWay}" SelectedItem="{Binding Path=OznacenaJedinica, Mode=TwoWay}" IsReadOnly="True" HorizontalAlignment="Left" Height="299" Margin="36,69,0,0" VerticalAlignment="Top" Width="496">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding ID}" Header="ID: " />
                <DataGridTextColumn Binding="{Binding Naziv}" Header="Naziv: " />
                <DataGridTextColumn Binding="{Binding Opstina.Naziv_Opstine}" Header="Opstina: " />
                <DataGridTextColumn Binding="{Binding Komandir.Ime}" Header="Komandir: " />
                <DataGridTextColumn Binding="{Binding Vozila.Count}" Header="Broj vozila: " />
                <DataGridTextColumn Binding="{Binding Radnici.Count}" Header="Broj radnika: " />
                <DataGridTemplateColumn MinWidth="100" MaxWidth="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Komandir, ConverterParameter='Dodaj Komandira', Converter={StaticResource NullToMessageConverter}}" Margin="5,5,5,5" cal:Message.Attach="DodajKomandira($dataContext)" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Label Content="Vatrogasne Jedinice" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Top" Width="272" Height="54" Margin="260,10,260,0" />
        <Button x:Name="Obrisi" Content="Obrisi" HorizontalAlignment="Left" Margin="566,69,0,0" VerticalAlignment="Top" Width="180" />
        <Button x:Name="Izmeni" Content="Izmeni" HorizontalAlignment="Left" Margin="566,103,0,0" VerticalAlignment="Top" Width="180" />
        <Button x:Name="Dodaj" Content="Dodaj" HorizontalAlignment="Left" Margin="566,137,0,0" VerticalAlignment="Top" Width="180" />
    </Grid>
</Window>
